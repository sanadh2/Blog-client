import React, { useContext, useEffect } from "react";
import { useNavigate } from "react-router-dom";
import { UserData } from "../Contexts/UserData";

const Search = () => {
  return <div className="min-h-[200vh] relative z-0"></div>;
};

export default Search;
